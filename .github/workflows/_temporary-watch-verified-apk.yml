name: Temporary Watch Verified APK

on:
  push:
    branches: [main]
    paths:
      - .github/watch-verified-apk-request.txt

permissions:
  actions: read
  contents: write

jobs:
  watch:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Watch latest Release APK
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sleep 8
          gh run list --repo "$GITHUB_REPOSITORY" --workflow release-apk.yml --limit 1 --json databaseId,status,conclusion,url,headSha,createdAt > initial.json
          id="$(jq -r '.[0].databaseId')"
          echo "Watching run $id"
          set +e
          gh run watch "$id" --repo "$GITHUB_REPOSITORY" --exit-status --interval 10
          watch_status=$?
          set -e
          gh run view "$id" --repo "$GITHUB_REPOSITORY" --json databaseId,status,conclusion,url,headSha,createdAt,jobs > final.json
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${id}/artifacts?per_page=100" > artifacts.json
          jq -n --slurpfile run final.json --slurpfile artifacts artifacts.json --argjson watch_status "$watch_status" '{watch_status:$watch_status,run:$run[0],artifacts:$artifacts[0].artifacts}' > verified-apk-status.json
      - name: Persist result and clean watcher
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          [ -s verified-apk-status.json ] || echo '{"error":"watcher status missing"}' > verified-apk-status.json
          path=.github/verified-apk-status.json
          sha="$(gh api "repos/${GITHUB_REPOSITORY}/contents/${path}?ref=main" --jq .sha 2>/dev/null || true)"
          content="$(base64 -w0 verified-apk-status.json)"
          args=(--method PUT "repos/${GITHUB_REPOSITORY}/contents/${path}" -f message='ci: save verified production APK status' -f content="$content" -f branch=main)
          [ -z "$sha" ] || args+=(-f sha="$sha")
          gh api "${args[@]}" >/dev/null
          req=.github/watch-verified-apk-request.txt
          req_sha="$(gh api "repos/${GITHUB_REPOSITORY}/contents/${req}?ref=main" --jq .sha 2>/dev/null || true)"
          [ -z "$req_sha" ] || gh api --method DELETE "repos/${GITHUB_REPOSITORY}/contents/${req}" -f message='ci: remove verified APK watch request' -f sha="$req_sha" -f branch=main >/dev/null
          self=.github/workflows/_temporary-watch-verified-apk.yml
          self_sha="$(gh api "repos/${GITHUB_REPOSITORY}/contents/${self}?ref=main" --jq .sha 2>/dev/null || true)"
          [ -z "$self_sha" ] || gh api --method DELETE "repos/${GITHUB_REPOSITORY}/contents/${self}" -f message='ci: remove temporary verified APK watcher' -f sha="$self_sha" -f branch=main >/dev/null
